# Personal (`user/`)

Personal contracts are not shipped as sample voices.

Check in a real self via issue or PR: `/personas/submit`.
On the public contract repo the file lives at `user/<slug>/PERSONA.md`.
