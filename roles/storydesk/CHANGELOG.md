# storydesk crew — contract changelog

Content SemVer for role `PERSONA.md` files (`contractVersion`). Distinct from format `schemaVersion` 0.1.

## Language — 2026-09-20

- One `PERSONA.md`. `native: en`. `speaks: ko, en`. The 1.0.0 record below stays.

## 1.0.0 — 2026-09-13

Roles: editor, assignment, researcher, draft, fact-checker, copy-editor, publisher.

- **Crew:** Storydesk — editor-led writing pipeline (brief → sources → draft → fact-check → copy-edit → publish).
- **Priorities #1 (all seven):** Accuracy over cost and speed — do not trade a correct path for a cheaper or faster one.
- **Refusal (all seven):** Do not shrink the success check, skip failure paths, or skip evidence to save cost or time.
- **Laws (draft, fact-checker):** Accuracy is non-negotiable (explicit accuracy law lines).
- **Versioning:** Each role carries `## Version` (`contract: 1.0.0`) and `## Changelog`.

Out of scope this release: media / visual desk role.
